<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".utils.MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GithubUserSearch"
        tools:targetApi="31">

        <!-- MainActivity should not be exported and should not have the LAUNCHER intent filter -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false">
            <!-- No intent filter here -->
        </activity>

        <!-- SplashScreenActivity should be exported and have the LAUNCHER intent filter -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- UserDetailActivity should be exported if it needs to be launched from outside the app -->
        <activity
            android:name=".ui.UserDetailActivity"
            android:exported="false">
            <!-- No intent filter here -->
        </activity>

    </application>

</manifest>
